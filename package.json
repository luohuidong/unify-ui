{
  "name": "unify-ui",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "build:vitepress": "pnpm build && pnpm -r build:vitepress",
    "build:storybook": "pnpm build && pnpm -r build:storybook",
    "cz": "cz",
    "cs": "changeset",
    "csv": "changeset version",
    "test": "pnpm -r test",
    "vitest": "vitest run",
    "format": "prettier --write packages/**/src",
    "format:check": "prettier --check packages/**/src"
  },
  "license": "MIT",
  "devDependencies": {
    "@changesets/cli": "^2.27.6",
    "commitizen": "^4.3.0",
    "cz-conventional-changelog": "^3.3.0",
    "prettier": "3.2.5"
  },
  "dependencies": {
    "vite": "^5.3.1",
    "vitest": "^1.6.0"
  }
}
