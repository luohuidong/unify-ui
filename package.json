{
  "name": "unify-ui-workspaces",
  "private": true,
  "scripts": {
    "build": "pnpm --filter 'unify-ui' build && pnpm --filter '@unify-ui/docs' build",
    "release": "pnpm build && pnpm -r publish",
    "test": "pnpm -r test",
    "cz": "cz",
    "cs": "changeset"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.2",
    "commitizen": "^4.3.0"
  }
}
